export default function SideBar() {
    return (
        <div>
            <button>Drinks</button>
            <button>Ingredients</button>
            <button>Meals</button>

            <button>Add New</button>
        </div>

    )
}